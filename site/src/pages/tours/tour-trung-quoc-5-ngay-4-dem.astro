---
/**
 * BẢN SAO ở CẤP GỐC — URL đầy đủ:
 *   https://todaytourist.com/tours/tour-trung-quoc-5-ngay-4-dem/
 *
 * Đây là URL CŨ (do site trước để lại) của trang thật:
 *   https://todaytourist.com/vi/tour/tour-trung-quoc-5-ngay-4-dem/
 *
 * Trang dùng LẠI đúng component `TourDetailPage` với cùng lang/slug nên phần thân
 * HTML giống hệt trang thật, và vẫn đọc chung
 * `content/vi/tours/tour-trung-quoc-5-ngay-4-dem.json` → sửa nội dung là cả hai
 * trang đổi theo (không có bản JSON trùng để lệch nhau về sau).
 *
 * Khác biệt duy nhất nằm ở `<head>` do BaseLayout sinh: canonical, hreflang,
 * breadcrumb, og:url và JSON-LD của bản sao đều trỏ về trang thật
 * (xem `ROOT_ALIASES` trong `src/lib/content.js`) → Google chỉ index 1 URL duy
 * nhất, không sinh duplicate content. Vì vậy path này cũng KHÔNG vào sitemap.
 *
 * BẮT BUỘC: path 'tours/tour-trung-quoc-5-ngay-4-dem' phải có trong
 * `ROOT_ALIASES` (src/lib/content.js), nếu không trang sẽ phát hreflang trỏ tới
 * '/vi/tours/<slug>/' và '/en/tours/<slug>/' (URL 404, không tồn tại).
 */
import TourDetailPage from '@/components/TourDetailPage.astro';

const LANG = 'vi';
const SLUG = 'tour-trung-quoc-5-ngay-4-dem';
---

<TourDetailPage lang={LANG} slug={SLUG} />
